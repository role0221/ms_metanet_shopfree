enum SORT_TYPE {
  ASC = `ASC`,
  DESC = `DESC`
}

enum SORT_BY {
  CREATED_AT = `createdAt`,
  UPDATED_AT = `updatedAt`,
  LOT_NUMBER = `lotNumber`,
}

export {
  /** ENUME */
  SORT_TYPE,
  SORT_BY


}
